# Icons

Place app and tray icons here:

- `tray.png` — system-tray icon (loaded by `src/main/tray.ts`; a missing file
  falls back to an empty image so startup never crashes).
- `icon.png` / `icon.ico` / `icon.icns` — app icons used by electron-builder
  during packaging.
